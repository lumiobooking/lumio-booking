# ===========================================================================
# Lumio Booking - Web dashboard environment (EXAMPLE)
# Copy to ".env.local" for local development. NEVER commit real secrets.
# ===========================================================================

# The dev dashboard runs on http://localhost:3005 (configured in package.json).
# This points the frontend at the backend API (port 8005).
NEXT_PUBLIC_API_URL=http://localhost:8005/api
